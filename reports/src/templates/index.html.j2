<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Diario Operativo — Dashboard</title>
  <style>
    :root{--bg:#0b0f19;--card:#121a2a;--text:#e7ecff;--muted:#a8b3d8;--accent:#77a7ff;}
    body{margin:0;font-family:ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial; background:var(--bg); color:var(--text);}
    .wrap{max-width:1100px;margin:0 auto;padding:24px;}
    .h1{font-size:28px;font-weight:800;margin:0 0 6px;}
    .muted{color:var(--muted);}
    .grid{display:grid;grid-template-columns:1fr;gap:16px;}
    @media(min-width:900px){.grid{grid-template-columns:1.2fr .8fr;}}
    .card{background:var(--card);border:1px solid rgba(255,255,255,.08);border-radius:16px;padding:16px; box-shadow:0 6px 22px rgba(0,0,0,.35);}
    table{width:100%;border-collapse:collapse;font-size:13px;}
    th,td{padding:8px 10px;border-bottom:1px solid rgba(255,255,255,.08);vertical-align:top;}
    th{color:var(--muted);font-weight:700;text-align:left;}
    a{color:var(--accent);text-decoration:none;}
    a:hover{text-decoration:underline;}
    .kpi{display:flex;gap:16px;flex-wrap:wrap}
    .kpi .pill{padding:8px 10px;border-radius:999px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.04);}
    .img{max-width:100%;border-radius:12px;border:1px solid rgba(255,255,255,.10);}
    .badge{display:inline-flex;align-items:center;gap:8px;padding:8px 10px;border-radius:999px;border:1px solid rgba(255,255,255,.12);background:rgba(255,255,255,.04);}
  </style>
</head>
<body>
<div class="wrap">
  <div class="h1">Diario Operativo — Dashboard</div>
  <div class="muted">Timezone: {{ tz }} · Rows: {{ meta.rows_daily }}</div>

  <div class="grid" style="margin-top:16px">
    <div class="card">
      <div style="font-weight:800;margin-bottom:10px">Heatmap (últimos días)</div>
      <img class="img" src="{{ heatmap_rel }}" alt="heatmap"/>
      <div class="muted" style="font-size:12px;margin-top:6px">Valores normalizados por métrica.</div>
    </div>

    <div class="card">
      <div style="font-weight:800;margin-bottom:10px">Accesos rápidos</div>

      {% if weekly and weekly|length > 0 %}
        <div class="badge" style="margin-bottom:12px">
          <span style="font-weight:800">Semana actual:</span>
          <span>{{ weekly[-1].week }}</span>
          <span>·</span>
          <span style="font-weight:800">{{ weekly[-1].status }}</span>
          <span class="muted">(avg {{ (weekly[-1].score_avg or 0) | round(2) }})</span>
        </div>
      {% endif %}

      <div class="kpi">
        {% for w in weekly %}
          <div class="pill"><a href="weekly_{{ w.week }}.html">{{ w.week }}</a></div>
        {% endfor %}
      </div>
      <div style="height:10px"></div>
      <div class="kpi">
        {% for m in monthly %}
          <div class="pill"><a href="monthly_{{ m.month }}.html">{{ m.month }}</a></div>
        {% endfor %}
      </div>
    </div>
  </div>

  <div class="card" style="margin-top:16px">
    <div style="font-weight:800;margin-bottom:10px">Últimos 30 días</div>
    <table>
      <thead>
        <tr>
          <th>date</th>
          <th>score</th>
          <th>energy</th>
          <th>sleep</th>
          <th>pomo_min</th>
          <th>focus_min</th>
          <th>alcohol_units</th>
          <th>stalk</th>
          <th>stalk_level</th>
          <th>stalk_intensity</th>
          <th>feature_done</th>
        </tr>
      </thead>
      <tbody>
      {% for r in daily %}
        <tr>
          <td>{{ r.date }}</td>
          <td>{{ r.score }}</td>
          <td>{{ r.energy }}</td>
          <td>{{ r.sleep_hours }}</td>
          <td>{{ r.pomodoro_minutes }}</td>
          <td>{{ r.focus_minutes }}</td>
          <td>{{ r.alcohol_units }}</td>
          <td>{{ r.stalk_occurred }}</td>
          <td>{{ r.stalk_intensity_label }}</td>
          <td>{{ r.stalk_intensity }}</td>
          <td>{{ r.feature_done }}</td>
        </tr>
      {% endfor %}
      </tbody>
    </table>
  </div>

  <div class="grid" style="margin-top:16px">
    <div class="card">
      <div style="font-weight:800;margin-bottom:10px">Resumen semanal</div>
      <table>
        <thead><tr>
          <th>week</th>
          <th>status</th>
          <th>days</th>
          <th>score_avg</th>
          <th>pomo_min</th>
          <th>focus_min</th>
          <th>alcohol</th>
          <th>stalk_days</th>
          <th>stalk_avg</th>
        </tr></thead>
        <tbody>
        {% for r in weekly %}
          <tr>
            <td><a href="weekly_{{ r.week }}.html">{{ r.week }}</a></td>
            <td>{{ r.status }}</td>
            <td>{{ r.days }}</td>
            <td>{{ (r.score_avg or 0) | round(2) }}</td>
            <td>{{ r.pomodoro_min }}</td>
            <td>{{ r.focus_min }}</td>
            <td>{{ r.alcohol_units }}</td>
            <td>{{ r.stalk_days }}</td>
            <td>{{ (r.stalk_intensity_avg or 0) | round(2) }}</td>
          </tr>
        {% endfor %}
        </tbody>
      </table>
    </div>

    <div class="card">
      <div style="font-weight:800;margin-bottom:10px">Resumen mensual</div>
      <table>
        <thead><tr>
          <th>month</th>
          <th>days</th>
          <th>score_avg</th>
          <th>pomo_min</th>
          <th>focus_min</th>
          <th>alcohol</th>
          <th>stalk_days</th>
          <th>stalk_avg</th>
        </tr></thead>
        <tbody>
        {% for r in monthly %}
          <tr>
            <td><a href="monthly_{{ r.month }}.html">{{ r.month }}</a></td>
            <td>{{ r.days }}</td>
            <td>{{ (r.score_avg or 0) | round(2) }}</td>
            <td>{{ r.pomodoro_min }}</td>
            <td>{{ r.focus_min }}</td>
            <td>{{ r.alcohol_units }}</td>
            <td>{{ r.stalk_days }}</td>
            <td>{{ (r.stalk_intensity_avg or 0) | round(2) }}</td>
          </tr>
        {% endfor %}
        </tbody>
      </table>
    </div>
  </div>
{% if ai_weekly %}
  <div class="card" style="margin-top:16px">
    <div style="font-weight:900;font-size:22px;margin-bottom:10px">🧠 Análisis IA (última semana)</div>

    {% if ai_weekly.theme %}
      <div class="badge" style="margin-bottom:14px">
        <span style="font-weight:800">Tema:</span>
        <span>{{ ai_weekly.theme }}</span>
      </div>
    {% endif %}

    {% if ai_weekly.evidence %}
      <div style="font-weight:800;margin:14px 0 8px">Evidencia</div>
      {% if ai_weekly.evidence.checkins_quotes and ai_weekly.evidence.checkins_quotes|length > 0 %}
        <div style="margin-bottom:8px">
          <div class="muted" style="font-size:13px;font-weight:700">Checkins:</div>
          <ul style="margin-top:4px">
            {% for q in ai_weekly.evidence.checkins_quotes %}
              <li class="muted" style="font-size:13px">"{{ q }}"</li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}
      {% if ai_weekly.evidence.diary_quotes and ai_weekly.evidence.diary_quotes|length > 0 %}
        <div style="margin-bottom:8px">
          <div class="muted" style="font-size:13px;font-weight:700">Diario:</div>
          <ul style="margin-top:4px">
            {% for q in ai_weekly.evidence.diary_quotes %}
              <li class="muted" style="font-size:13px">"{{ q }}"</li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}
    {% endif %}

    <div style="font-weight:800;margin:14px 0 8px">Patrones</div>
    <ul>
      {% for x in (ai_weekly.patterns or []) %}
        <li>{{ x }}</li>
      {% endfor %}
    </ul>

    <div style="font-weight:800;margin:14px 0 8px">Victorias</div>
    <ul>
      {% for x in (ai_weekly.wins or []) %}
        <li>{{ x }}</li>
      {% endfor %}
    </ul>

    <div style="font-weight:800;margin:14px 0 8px">Cuellos de botella</div>
    <ul>
      {% for x in (ai_weekly.bottlenecks or []) %}
        <li>{{ x }}</li>
      {% endfor %}
    </ul>

    <div style="font-weight:800;margin:14px 0 8px">Deep dive de Checkins</div>
    {% if ai_weekly.checkins_deep_dive and ai_weekly.checkins_deep_dive|length > 0 %}
      {% for item in ai_weekly.checkins_deep_dive %}
        <div style="padding:12px;border:1px solid rgba(255,255,255,.08);border-radius:12px;margin-bottom:10px;background:rgba(255,255,255,.03);">

          <div style="margin-top:6px"><b>Interpretación:</b> {{ item.interpretation }}</div>
          <div style="margin-top:6px"><b>Disparador:</b> {{ item.trigger_guess }}</div>
          <div style="margin-top:6px"><b>Señal corporal:</b> {{ item.body_signal }}</div>
          <div style="margin-top:6px"><b>Contra-movida (2–5 min):</b> {{ item.counter_move_2_5_min }}</div>
        </div>
      {% endfor %}
    {% else %}
      <div class="muted">Sin suficiente data de checkins para deep dive.</div>
    {% endif %}

    <div style="font-weight:800;margin:14px 0 8px">Reglas para la próxima semana</div>
    <ul>
      {% for x in (ai_weekly.next_week_rules or []) %}
        <li>{{ x }}</li>
      {% endfor %}
    </ul>

    <div style="font-weight:800;margin:14px 0 8px">Si fallo, entonces...</div>
    <ul>
      {% for x in (ai_weekly.if_i_fail_then or []) %}
        <li>{{ x }}</li>
      {% endfor %}
    </ul>

    {% if ai_weekly.identity_sentence %}
      <div class="muted" style="margin-top:12px">
        <b>Identidad (operacional):</b> {{ ai_weekly.identity_sentence }}
      </div>
    {% endif %}
  </div>
{% endif %}
  <div class="muted" style="margin-top:16px;font-size:12px">
    Generado por el CLI. Ajusta scoring y columnas en <code>src/diario/scoring.py</code>.
  </div>
</div>
</body>
</html>
